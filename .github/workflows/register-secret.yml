name: Dummy Workflow to Register Secret

on:
  workflow_dispatch:  # Manual trigger only, won't run automatically

jobs:
  dummy:
    runs-on: ubuntu-latest
    steps:
      - name: Reference secret (does nothing)
        run: echo "Secret is registered"
        env:
          REPO_ACCESS_SECRET: ${{ secrets.REPO_ACCESS_SECRET }}
